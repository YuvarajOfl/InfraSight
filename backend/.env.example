# App Settings
APP_NAME="InfraSight Auth Backend"
APP_ENV="development"
PORT=8000

# Database Settings (Assumes MySQL default locally, falls back to SQLite internally if unavailable)
DB_USER="root"
DB_PASSWORD="rootpassword"
DB_HOST="127.0.0.1"
DB_PORT="3306"
DB_NAME="infrasight"

# Authentication Settings
JWT_SECRET="43b1c1e5a5f4fcd3116df1b3152a5170d742617f698e82ef6fae54ab0d45bf86"
JWT_ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Google OAuth Client Credentials
# Create yours in Google Cloud Console and paste it here
GOOGLE_CLIENT_ID="915887390862-fcgaqrabnob077qjicmpaf79db80cqic.apps.googleusercontent.com"

# CORS Origins Allowed (comma separated list)
CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173"
